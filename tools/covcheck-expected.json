{
 "scope": "crates/before/src/version/skyline/",
 "line": {
  "crates/before/src/version/skyline/admit.rs": [
   {
    "anchor": "None => unreachable!(",
    "disposition": "panic-arm",
    "why": "the walk checks done() before advancing, so the pop always finds an open ancestor"
   }
  ],
  "crates/before/src/version/skyline/build.rs": [
   {
    "anchor": "a leaf arrived after the final plateau",
    "disposition": "panic-arm",
    "why": "the builder's tiling is complete at the final plateau; a later leaf is programmer error"
   }
  ],
  "crates/before/src/version/skyline/fill.rs": [
   {
    "anchor": "fill is idempotent: a filled tree cannot fill again",
    "disposition": "panic-arm",
    "why": "the committed fill_is_idempotent differential pins the second walk unchanged"
   },
   {
    "anchor": "IdNode::Empty => {",
    "offset": 1,
    "disposition": "unreachable",
    "why": "reachable only at an empty root, and no public Party is ever empty: finish_id in party.rs is the single gate every parsed/built Party passes and rejects the anonymous identity (Parse::Anonymous), and Party::without returns None rather than an empty share; below the root the walk descends only into present children. The arm realizes fill(0, e) = e totally."
   },
   {
    "anchor": "IdNode::Empty => {",
    "offset": 2,
    "disposition": "unreachable",
    "why": "the empty-root arm's cost, with the line above"
   },
   {
    "anchor": "a consumed site keeps a relation",
    "disposition": "panic-arm",
    "why": "every site consume installs a Height or Min relation before the site closes"
   }
  ],
  "crates/before/src/version/skyline/fill/fuse.rs": [
   {
    "anchor": "a verbatim emission is matched or has diverged",
    "disposition": "panic-arm",
    "why": "matched emissions return before their bodies and diverging ones materialize first (the method's Panics section)"
   },
   {
    "anchor": "the region splice runs post-divergence",
    "disposition": "panic-arm",
    "why": "continue_verbatim runs only after a divergence built the output"
   },
   {
    "anchor": "Out::Built(builder) => return Some(builder.finish()),",
    "offset": 1,
    "disposition": "unreachable",
    "why": "finish sees Unstarted only for a stream with no plateaus, and a canonical skyline stream holds at least one leaf (the codec validation contract); the debug_assert beside the arm pins matched_end == event.len()"
   },
   {
    "anchor": "a present id child is a real node",
    "disposition": "panic-arm",
    "why": "the walk descends only into children whose presence bits are set"
   }
  ],
  "crates/before/src/version/skyline/fill/prescan.rs": [
   {
    "anchor": "IdNode::Empty => {",
    "offset": 1,
    "disposition": "unreachable",
    "why": "the pre-scan enters at a present child (a left-full site's right sibling, peeked present at the fill.rs call site), so only an empty root could read Empty, and the entry is never the root; the arm realizes min(fill(0, e)) = min(e) totally"
   },
   {
    "anchor": "IdNode::Empty => {",
    "offset": 2,
    "disposition": "unreachable",
    "why": "the empty-root arm, with the line above"
   },
   {
    "anchor": "IdNode::Full => {",
    "offset": 1,
    "disposition": "unreachable",
    "why": "every entry hands in a full child's sibling (never full: a (1, 1) node collapses under idbits' normal form) or a child the caller peeked as not-full; the inline comment carries the argument"
   },
   {
    "anchor": "IdNode::Full => {",
    "offset": 2,
    "disposition": "unreachable",
    "why": "the full-arm body, with the line above"
   },
   {
    "anchor": "IdNode::Full => {",
    "offset": 3,
    "disposition": "unreachable",
    "why": "the full-arm body, with the line above"
   }
  ],
  "crates/before/src/version/skyline/grow.rs": [
   {
    "anchor": "a full id over an event node collapses under fill",
    "disposition": "panic-arm",
    "why": "grow inflates only the unchanged branch, where a fully-owned terminal covers a single event leaf: a full id over an event node collapses under fill and trips the changed flag first, so the inflation walk never meets one"
   }
  ],
  "crates/before/src/version/skyline/masked.rs": [
   {
    "anchor": "Self::B_MASK => self.b_mask.as_ref().map_or(0, PlateauCursor::depth),",
    "offset": 1,
    "disposition": "panic-arm",
    "why": "advance_set steps only slots the roster's priority() yields (the overlay advance law), and the roster names exactly four"
   },
   {
    "anchor": "Self::B_MASK => {",
    "offset": 7,
    "disposition": "panic-arm",
    "why": "advance_set steps only slots the roster's priority() yields (the overlay advance law), and the roster names exactly four"
   }
  ],
  "crates/before/src/version/skyline/overlay.rs": [
   {
    "anchor": "the advanced cursor is never at its final leaf: an all-right path means the stream is consumed",
    "offset": -1,
    "disposition": "panic-arm",
    "why": "the sweep stops when both cursors are done; the module doc's bookkeeping shows a final leaf is never the advanced side"
   },
   {
    "anchor": "the advanced cursor is never at its final region: an all-right path means the stream is consumed",
    "offset": -1,
    "disposition": "panic-arm",
    "why": "the region walk's twin of the leaf walk's final-leaf invariant"
   }
  ],
  "crates/before/src/version/skyline/place.rs": [
   {
    "anchor": "Self::END => self.end.as_ref().map_or(0, |side| side.cursor.depth()),",
    "offset": 1,
    "disposition": "panic-arm",
    "why": "advance_set steps only slots the roster's priority() yields (the overlay advance law), and the roster names exactly three"
   },
   {
    "anchor": "Self::END => Self::step_bound(&mut self.end),",
    "offset": 1,
    "disposition": "panic-arm",
    "why": "advance_set steps only slots the roster's priority() yields (the overlay advance law), and the roster names exactly three"
   }
  ],
  "crates/before/src/version/skyline/watermark.rs": [
   {
    "anchor": "unreachable!(\"undercut residues are strictly positive\")",
    "disposition": "panic-arm",
    "why": "a violated positivity invariant panics: stacked differences and undercut residues are strictly positive by the propagation discipline, so a decided non-Greater domination read is programmer error, never a foldable state"
   },
   {
    "anchor": "unreachable!(\"stacked differences are strictly positive\")",
    "disposition": "panic-arm",
    "why": "a violated positivity invariant panics: stacked differences and undercut residues are strictly positive by the propagation discipline, so a decided non-Greater domination read is programmer error, never a foldable state"
   }
  ]
 },
 "branch": {
  "crates/before/src/version/skyline/build.rs": [
   {
    "anchor": "first_rel_depth >= 1 && last_rel_depth >= 1,",
    "disposition": "panic-arm",
    "why": "the untaken directions are the assertion's failure arms"
   }
  ],
  "crates/before/src/version/skyline/fill.rs": [
   {
    "anchor": "self.phase.last() == Some(false) && self.site.last() == Some(false),",
    "disposition": "panic-arm",
    "why": "the untaken directions are the assertion's failure arms"
   }
  ],
  "crates/before/src/version/skyline/fill/prescan.rs": [
   {
    "anchor": "self.phase.last() == Some(false) && self.site.last() == Some(false),",
    "disposition": "panic-arm",
    "why": "the untaken directions are the assertion's failure arms"
   }
  ],
  "crates/before/src/version/skyline/masked.rs": [
   {
    "anchor": "directions.le && directions.ge,",
    "disposition": "panic-arm",
    "why": "the untaken directions are the assertion's failure arms"
   }
  ],
  "crates/before/src/version/skyline/sweep.rs": [
   {
    "anchor": "directions.le && directions.ge,",
    "disposition": "panic-arm",
    "why": "the untaken directions are the assertion's failure arms"
   }
  ],
  "crates/before/src/version/skyline/text.rs": [
   {
    "anchor": "open.is_empty() && lefts.is_empty(),",
    "disposition": "panic-arm",
    "why": "the untaken directions are the assertion's failure arms"
   }
  ],
  "crates/before/src/version/skyline/place/filter.rs": [],
  "crates/before/src/version/skyline/query/integral.rs": [
   {
    "anchor": "digit != 0 && digit.unsigned_abs() <= 1 << 31,",
    "disposition": "panic-arm",
    "why": "the untaken directions are the assertion's failure arms"
   }
  ],
  "crates/before/src/version/skyline/signed.rs": [
   {
    "anchor": "!sign.is_negative() || magnitude != Base::ZERO,",
    "disposition": "panic-arm",
    "why": "the untaken direction is the assertion's failure arm"
   },
   {
    "anchor": "!sign.is_negative() || *magnitude != Base::ZERO,",
    "disposition": "panic-arm",
    "why": "the untaken direction is the assertion's failure arm"
   },
   {
    "anchor": "!sign.is_negative() || *mag != 0,",
    "disposition": "panic-arm",
    "why": "the untaken direction is the assertion's failure arm"
   }
  ]
 }
}