# The constants frontier

Ideation, nothing committed: candidate directions for a future campaign on
`before`'s per-bit constants, once the asymptotic work had taken the exponents
as far as they go. Every cost is denominated against the closeout judge table
and every yield is explicitly labeled an estimate, to be refuted or confirmed by
a probe before anything is committed to.

- [`before-constants-frontier.md`](before-constants-frontier.md) — where the
  baseline sits (§1), candidate directions (§2), additional thoughts (§3), and
  what not to do and why the attempt is safe at all (§4).

---

Resurrected from `design/before-constants-frontier.md`, written 2026-07-27, retired in `e13854de` (Remove outdated design docs). The body below is verbatim: its `design/…` cross-references resolve through the [migration map](../2026-08-19-design-directory-migration/README.md).
