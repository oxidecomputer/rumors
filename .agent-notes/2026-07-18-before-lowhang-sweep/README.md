# `before`: the low-hanging-fruit sweep

A one-day audit of `before`'s hot surface for cheap in-session wins, done by
AND-filtered session profiles plus an exhaustive file-by-file read. Its headline
is a negative result worth keeping: after the D-ladder landed, no ms-scale
in-session fruit remained at I = 5000. The "swept and found clean" section
exists so the same ground is not re-audited.

- [`before-lowhang-sweep.md`](before-lowhang-sweep.md) — headline, finds in
  impact-per-risk order, the clean list, and the same-day disposition.

---

Resurrected from `design/before-lowhang-sweep.md`, written 2026-07-18, retired in `218b254a` (ghost excision: the two dated probe reports no longer reference deleted code). The body below is verbatim: its `design/…` cross-references resolve through the [migration map](../2026-08-19-design-directory-migration/README.md).
