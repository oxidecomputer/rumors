<!-- CAVEAT LECTOR: written by Claude (Fable 5.1) for Finch during the rumors triage of 2026-09-02; findings surfaced by lanes and fresh-eyes reviews that no roster entry covers, each with its disposition or the ruling that gave it one; not authored, audited, or endorsed by Finch. Read with the ground rules in ../README.md. -->

# Findings without an entry

A finding a lane or reviewer noticed that the review's roster does not
carry gets a row here the day it is found, with its evidence and its
disposition: a ruling that homes it, a lane that lands it, or a stated
reason it changes nothing. Nothing is mentioned and left.

| found by | finding | evidence | disposition |
|---|---|---|---|
| p1-harness-tests lane | After a complete handshake, a responder whose peer dies before opening its first data stream parks in the data-stream accept and never consults control EOF. Within the link contract (the caller owns the timeout); a cheap death signal ignored. | the lane's probe log (`STUCK ... reads: 6 ... connects: 0, accepts: 0`, no EOF line) | T143 recorded it as an open item with the in-process vanish fault as its instrument (committed ignored, failing on the current tree with `Stalled`); T145 fixes it: the session observes the control stream while awaiting a data stream, and the vanish arm draws every offset and asserts no survivor parks. Lane `p2-vanish-liveness`. |
| gate lane, fresh-eyes round 2 | `tools/doclint` walks its five roots with a bare `rglob("*.rs")` and no ignore set, so untracked build trees under `crates/` (`crates/before/fuzz/target`, `surfacecheck/target`, `wasm32-pins/target`, restored from cache in CI before `just ci` runs it) can change its verdict: the same class `verification-infra-9` fixed for testdoc. | `tools/doclint:370-373` at `b1a12f97`; `ci.yml` cache-restore steps precede the lint | Open. Candidate for the P3 lints lane (`p3-lints`), which already owns doclint's roster; the fix is testdoc's: the same ignore set, or a walk over `git ls-files`. |
| p2-walk lane | `tests/faults.rs`'s in-process `materialized_backend_failures_are_fail_fast` draws from `arb_divergent_pair`, whose few-leaf trees rarely dispute a scope, so the response-stream path is rarely reached; its wire twin was inert under a park mutation for the same reason until drawn from `arb_wide_divergent_pair`. | the lane's mutant run (`p2-walk` scratchpad logs) | Open. Candidate for the P5 tests lane: draw from the wide generator and record the mutant killing it. |
| p2-walk lane | `src/tree/mirror/streaming/materialized/levels.rs`, `internal_level`'s doc: "a mid-loop failure here would strand the counterparty's reply pump on a full slot, ahead of the error's own publication"; the wire twin exercises mid-loop backend failures across stages and every one returns, so the premise is undemonstrated. | the lane's `opening_failure_before_the_first_yield_returns_over_the_wire` and `materialized_backend_failures_are_fail_fast_over_the_wire` runs | Open. Candidate for the P4 prose lane over `materialized/`: demonstrate or delete the sentence. |
| p2-walk lane | `Resolver::new` takes an owned `Recorder`, so every request clones `stats` (four sites, the terminal leg included); borrowing it (`&Recorder`, as the ledger and version already are) removes the clone with no behavior change. | `src/tree/mirror/streaming/materialized.rs` at `18ebb1ad` | Open. A behavior-preserving nit for the P5 module lane over `materialized/`. |
| coordinator, reading the p2-link branch | The routed link's seam with a pooling transport is one rule (a pooling `Dial` reuses a recycled connection only after the peer's router writes its `READY` byte, dialing fresh otherwise), stated in `Dial::recycle`'s doc; the router's side is tested, but no committed transport implements a pooling `Dial`, so the rule has no conformance case in the reuse direction. | `src/link/routed.rs` at `de1c0dee`: the `recycle` doc; the link conformance suite's dials are the memory and TCP ones with the default drop | T153: the P5 link lane's roster (its brief carries this row). |
| coordinator, reading the p2-link branch | Pool sizing across layers: under T160 the router admits at most one session complement of idle connections per link, so a transport pool never needs more than a complement per link; the `recycle` doc must say so. | `src/link/routed.rs` `recycle` doc | Landed by the re-scoped `p2-link` at `48bcd486` (the corollary stated at `Dial::recycle`); T153's second clause is closed there, and the P5 link lane keeps only the pooling-`Dial` conformance case. |
| p1-causality lane (successor agent) | The justfile passes `--locked` to no cargo invocation, so a gate run on a tree whose `Cargo.lock` lags its manifest silently updates the lock in the build directory and passes; the causality lane's added `rand_chacha` dev-dependency reached the lock only when `cargo metadata --locked` was run by hand. | `justfile` at `main`: `git grep -c -- --locked justfile` is 0; the lane's `commit_round2.txt` | T155: the P3 lints lane lands `--locked` on every gate and `ci` cargo invocation with a lint-tier check. |
| p2-walk lane, fresh-eyes round 1 | The adjectival kin of the "fails typed" family ("typed violation / rejection / truncation / defect / diagnosis / EOF / session error / depth case") survive at 33 sites outside the lane's files; regenerating grep `\btyped\s+(fail|abort|error|violation|rejection|truncation|defect|diagnosis|EOF|session|depth|hand-off|intent|network)` over `src tests benches examples justfile`, excluding the type-level sense ("height-typed", "typed facade"). | the lane's report at `eb97720b` lists every site | Open. The P3 vocabulary lane (`p3-vocabulary`) widens its grep to this family. |
| `before` coordinator, on main after the walk merge; widened by the rumors coordinator | `just check` (default features) failed on main in two places. The walk lane's rebase over the harness-crate rewrite of `proxy/tests.rs`'s `use` block kept the walk's code but dropped its imports (`PayloadCodec`, `PayloadDepthLimit`, `FailingNode`, `RemoteHandshaking`, `MaterializedError`, `MirrorError`), its `failing_root` helper, and its `LocalFailure`/`LeftFailure`/`RightFailure` aliases. The causality lane's `tests/bookmark_causality.rs` calls `FaultPlan::is_clean`, which the harness-tests lane's round-2 commit `2d3906cf` deleted (its only caller was the dissolved inter-process family) before the causality lane rebased over it. Neither merged tree was compiled after its rebase. | `diff <(git show eb97720b:src/tree/mirror/streaming/remote/proxy/tests.rs) <(git show 44a26d70:...)`; `git log -S'fn is_clean' main -- tests/common/fault.rs`; the box log `coordinator/repair-main/verify.log` | Repaired on `main` as a merge-integration commit naming both lanes (the walk's pieces restored verbatim; `is_clean` replaced by `!= FaultPlan::NONE`, its exact former body). Process: WORKFLOW.md's merge step now compiles the rebased tip at default and all features before the fast-forward. Gate: a default-features `cargo check --workspace --all-targets` leg in the lint tier is routed to `p3-lints` with T155 (the justfile is `before/p1-gate`'s this wave). |
| p2-commit-path lane | `tests/encode_alloc.rs`'s `full_fan_query_allocations` failed once on the box (`left: 6 right: 2`) inside a gate's `test-all` at a one-minute load near 450, and passed on every other run; nothing in the lane or in main's commits touched the pin or the codec. A deterministic allocation count that moves with load is a finding about the pin: a process-global counter catching another thread's allocation, or a lazy initialization inside the counted region. | the lane's scratchpad log `p2-commit-path/gate4b-*.log` | Open. Candidate for the P5 tests lane: make the counter thread-local or the region free of lazy initialization, and commit the shape that reproduces the drift. |
| p2-commit-path lane | The commit walk's reassembly merges the two ascending runs (as T39 names); an insert-back alternative (binary-search each recursed child back into the fan and drop the per-level `updated` buffer) would save one `Vec` per level. | the lane's report at `3447206f` | Not taken: the ruling names the merge. Recorded for the P7 slice-recursion redesign (T107). |
| p1-proptest-ci lane's release-profile measurement | `codec::decode::tests::unordered_query_is_rejected` aborts at `PROPTEST_CASES` of 1000 and above with `Too many global rejects`: its `prop_assume!(previous >= radix)` rejects half of all draws, so proptest's default cap of 1024 global rejects is reached before 1000 successes. The test is not wrong at the default count; its generator wastes half its budget and stops scaling. | the lane's `run-1000.log` (`successes: 956 ... global rejects: 1024 ... previous >= radix`) and `run-4000.log` | T157: generators draw their constraints; `prop_assume!` only where a constraint cannot be generated, with the reason at the site, and a lint-tier check holding the count. The P5 tests lane sweeps every site. Until then the CI release job's case count is bounded by this test's rejection budget, which the lane reports with its measurement. |
| p1-proptest-ci lane's release-profile measurement | Seven `before::meter` envelope rows fail under the release profile at every case count (`join_cliff`, `skyline_join_cliff`, `skyline_project_comb_scatter`, `skyline_render_cliff`, `skyline_render_hugeleaf`, `tick_expand_cross`, `tick_expand_spine`): the limb counter reads below its improvement tripwire (0.75 of the dev pin), because `debug_assert!` comparisons in the library's big-integer paths are metered limb operations that vanish under release; the suite's header states its pins are dev-profile. Two `before` proptests (`version::tests::grow_matches_brute_force`, `grow_minimal`) hit the global reject cap at 1000 cases and above, the same class as the rumors row above. | the lane's `run-1.log` through `run-4000.log`; `crates/before/tests/meter.rs` header ("The pins are dev-profile") | Relayed to the before triage (its rows). For this triage: the release job excludes `binary(meter)` with the reason in the recipe, and the case count is measured against the remaining suites. |
| p1-proptest-ci lane's release-profile measurement, 16000 cases | The inter-process disruption property `inter_process_disruption_upholds_party_invariants` (deleted from main by T143) failed once with `live parties must stay pairwise disjoint at every instant: peers 0 and 1 overlap ((1, 0) vs ((0, 1), 1))`, shrunk to a fault-free plan: two parent peers, no seed messages, one child making three sends. The seed did not reproduce on replay (nondeterministic interleaving). Party disjointness is the invariant the crate rests on, and the harness that reported it is the one T143 dissolved for its own accounting defects, so the report is either a real linearity defect or a harness artifact. | the lane's `run-16000.log`; the lane's commit `84c6b5c6` (a seed for the deleted property, not merged: its plan is quoted here instead) | Resolved: a harness artifact. The deleted prober read the parent process's handles sequentially, one `watch` read lock each, and its soundness claim ("no interleaving of these per-peer aliases can witness one region twice") holds for one instant, not for two reads: the reported pair is cast 0 read before the child's fork beside cast 1 read after the child's retirement, the only pair that index order can produce for this plan. Evidence: the exact plan replayed in one process on `main` with every party read from its replica after every step, disjoint at every step, 200 release runs, 0 failures; the report's message reproduced verbatim by reading parent 0, running the child's lifetime, then reading parent 1 (`tests/party_transfer_sampling.rs` on branch `triage/party-overlap` at `3978a4c5`, evidence, not landable prose); 50 sequential and 40 concurrent instrumented replays of the deleted harness, 0 overlaps, with the prober's read gap measured up to 186 ms under load against a child lifetime as short as 4 ms; and a reading of every fork, retire, and absorb path in `src/peer/gossip.rs`: a region is in exactly one of donor, guard, wire, or absorber at every instant, with `PartyGuard` re-joining on every early return. Follow-up, routed to the lane next on `tests/common/sim.rs` (vanish-liveness, or the P5 harness lane): the same prober and the same false soundness sentence survive on `main` (`sim.rs:822-830`); its reads need a consistent snapshot or a re-read confirmation, and the sentence is corrected. |
| p1-proptest-ci lane | `tests/party_conservation.rs`'s `fleet_scale_retirement_collapses_to_seed_party` took 177 s in the gate of record (debug, default 256 cases, box load 90 to 200), three seconds under nextest's 180 s kill; in release it takes 21 s. Under T151 no per-site count may shorten it, so it is a finding about the test's cost. | the lane's `gate6` log | Open, for the P5 tests lane: make the property's per-case work proportional to what it asserts (a smaller fleet per case, or a cheaper retirement fixture), and commit the shape. |
| coordinator, reading the lane's stop | A proptest's reject budgets (`max_global_rejects` 1024, `max_local_rejects` 65536) are fixed while `PROPTEST_CASES` scales, so a release job at a large count fails on every `prop_assume!`-heavy property before any real counterexample. | the lane's measurement table (3 reject-budget failures at 1000 and 4000, 5 at 16000) | T157 (generators draw their constraints); the CI count is chosen after the sweep, and `unordered_query_is_rejected` is rewritten in the proptest-ci lane so the job is green at its number. |
| p1-collision-mode lane (T23), under the schedule at seed 1 | `proxy/tests/declarations.rs::understated_version_bytes_fail_the_session`: with the understated-length lie heard on the right, the receiver reports `Decode(OversizedVersion { declared: 0, actual: 4 })` as the contract says; heard on the left, neither endpoint surfaces the typed error: the left reports `Stream(SupplyClosed { .. UnexpectedEof, "peer link is gone" })` and the right `Send(Frame(EncodeError { .. SupplyLength .. BrokenPipe }))`. Either an error-attribution race among the many concurrent streams a deep session opens, or a miss of the check on one side. Fixture: `early_first_child_dispute_pair()` under seed 1. | the lane's `sweep2-gate1.log`; branch `triage/p1-collision-mode` at `7858b35e` | Open, correctness class: a typed-error contract that holds under the hash's shallow geometry and not under deep geometry. Routed to a P2 lane after Finch's ruling on the collision-mode stops; the mode's recipe is the reproduction. |
| p1-collision-mode lane (T23), under the schedule at seed 1 | `streaming/tests.rs::cancelled_session_leaves_no_residue` failed once (passed on the first sweep) with `the measured session length is not reproducible` for a shrunk input of three leaves under a shared 31-byte prefix, every path memoized before either measurement: within one process the same input's poll count differed between the strategy's measurement and the test's run, so the session's poll schedule looks nondeterministic under deep geometry (the lane's first guess: `HashMap` iteration order among pending scopes at one level). No seed persisted on the box; the shrunk input is in the log from line 505. | the lane's `sweep2-gate1.log` line 505 on | Open, correctness class for the deterministic-driver premise the closed-world poller and the residue test rest on: a session's schedule must be a function of its input. Routed with the row above. |
| p1-collision-mode lane (T23) | `tests/listen.rs::folding_delivered_versions_can_lose_a_message` samples one point: its 256-universe search varies only the network identifier (the seed party and both versions are identical in every universe), so it passes under the hash only because SHA3 orders `[184]` before `[168]`; under the schedule the ordering flips and the search fails. | the lane's diagnostic (`items [(0, [168]), (1, [184])]` for every seed in both modes) | Open, for the P5 tests lane: the search varies the versions (tick counts or `nth_party` chains) so both orderings are sampled; T161 applies to the rewrite. |
| p1-collision-mode lane (T23) | Under deep geometry the disruption envelope session moves about 6177 B per endpoint and opens 17 data streams, but the fault plan's `MAX_CUT` and `MAX_VANISH_STREAM` are constants calibrated to the hash-shaped session, so the generator never cuts past offset 3072 or vanishes a stream past index 2: the deep tiers get no disruption coverage. | the lane's marks in `tests/disruption.rs` | Open, for the vanish-liveness or P5 harness lane: derive the fault ranges from the measured envelope, not constants. |
| p1-collision-mode lane (T23) | `conformance/link/tests.rs::starved_pool_degrades_latency_not_liveness` stalls under the schedule (`deep sessions stay live over a 64-byte pooled budget: Stalled`): a sub-bound pool couples streams, and the pin is a measured tolerance outside the link contract; marked as hash-only by the lane, but T31 and T160 make pooled dials first-class. | the lane's sweep logs | Open, for Finch with the collision-mode stops: whether a starved pool's liveness is a contract claim (then the stall is a defect for the link lane) or a tolerance (then the mark stands). |
| p2-commit-path fresh-eyes round 2 | The join walk's sink traffic is unmetered: no committed allocation pin covers a gossip or join commit (`tests/commit_alloc.rs` meters send and redact only), so a regression in the join path's per-level pushes (O(fan x depth) at a wide divergence) would pass the gate. | the reviewer's reading of `src/tree/traverse/join.rs:198-199` at `9ca5e959` | Open, for the P5 tests lane: a join allocation pin in `tests/commit_alloc.rs` at a measured wide-fan divergence, landed at the pre-fix number and tightened by any fix, per the send pins' shape. |
| p2-link lane (T160 re-scope) | The TCP demonstration's failure surface is platform-dependent: on illumos the dead pooled socket fails the payload write (`EPIPE`); on macOS the write succeeds and the accept never fires (`Elapsed`). Same defect, two surfaces; the test's doc states both with provenance. | the lane's `before-base-router-2.log` | Recorded; no action. |
| p2-link fresh-eyes round 1 (T160 shape) | A transport pool keys by peer (`Dial::recycle` receives no token), so when one link ends and the router releases its idle connections, another healthy link to the same peer may already be drawing one of them and fails that stream as transport failure, healing at session granularity; self-inflicted and unstated. Three shapes: state the hazard at `Dial::recycle`; drop the release and let a dead link's idle set drain as the peer's pool uses or drops it; carry the token into `recycle` so a pool can key by link. Also: the router's `drive` select must stay unbiased (a biased order starting at accept starves the return mailbox under a dial flood), the one session-adjacent site where fairness beats determinism. | the reviewer's schedule at `7a31b673`; `src/link/routed/router.rs` | Stop for Finch in the p2-link packet; the hazard sentence lands now as the reversible default; the two-links-one-peer pooling test lands with his choice. The unbiased `drive` select is documented at the site by the lane. |
| p1-collision-mode lane, round 1 at the 28-byte width (T163), seed 1 | `remote/proxy/tests/malformed::duplicated_reply_is_rejected_as_unasked` stalls under deep geometry: "duplicated final reply must terminate both sessions: Stalled". A correctness-class liveness defect in the same family as T162's finding 8: a malformed frame at a deep dispute is not surfaced as a session end. | the lane's `sweep` logs at `e951b81e`; `just test-collision` | Open, correctness class: joins the `p2-deep-geometry` lane's roster (its brief is re-pointed by the coordinator); at 28 bytes this is the recipe's reproduction. |
| p1-collision-mode lane, round 1 | At the 28-byte width, T162's findings 8 (`understated_version_bytes_fail_the_session`) and 9 (`cancelled_session_leaves_no_residue`) do not reproduce; both pass. They are behaviors of height-one disputes (31-byte shared prefixes), which are legal trees, so they remain correctness-class defects reachable at that geometry; the recipe at 28 bytes no longer reaches them, and the investigation lane's tree (`7858b35e`, 31 bytes) still does. Also at 28 bytes `starved_pool_degrades_latency_not_liveness` passes, dissolving the pooled-liveness question. | the lane's round-1 report | The `p2-deep-geometry` lane keeps 8 and 9 on its 31-byte base and adds the duplicated-reply stall; whether the recipe should also carry a 31-byte seed so those stay covered is a stop for Finch with the census question below. |
| p1-collision-mode lane, round 1 | Deep-streams census at 28 bytes: side A reaches stream index 15, side B 14 (16 both sides at 31 bytes): a same-cluster dispute descends to depth 28, and the last two stream indices are reached only by chance agreement in the free bytes. | the lane's census line in commit `ffedd47b` | Stop for Finch: whether depth 28 is "the deepest streams the protocol opens" (T163's premise), or streams 15 and 16 need structural coverage (a second cluster width, or a 31-byte seed in a sweep recipe). |
| p1-collision-mode fresh-eyes round 2 | The mode moves two geometric parameters at once and covers one band: eight clusters at one prefix length give an eight-child root and branching only at the bottom heights, so root-level batching and multi-subtree pipelining are exercised less under the schedule than under the hash, and several marks follow from the narrow root rather than from depth. | the reviewer's reading of `schedule.rs` at `e951b81e` | Design proposal for the P5 tests lane: per-cluster prefix lengths drawn from `1..=31` at the seed, with enough clusters to keep the root wide; a stop in the collision packet. |
| p2-link lane, a gate at `575bcc63` | `fuzzfit-harness::enforce fuel_stays_in_the_pinned_bands` failed on the `wasm` stream: `ff_party_decode` at 144 bits consumed 14460 fuel against a pinned band of about 10^3.882 (+0.075/-0.033); deterministic on replay of the box-written seed; the lane's diff touches nothing under `crates/`, and a second gate drew a different program and passed. A real, latent counterexample to before's pin. | the seed line committed as evidence at `triage/stops/p2-link/fuzzfit-enforce-seed-line.txt` on `triage/p2-link`; the lane's `r3-gate.log` | Relayed to the `before` triage (its instrument; the seed's tracked home is `crates/before/fuzzfit/.../enforce.txt`). |
| p2-deep-geometry lane | `malformed::duplicated_reply_is_rejected_as_unasked` stalls at the 28-byte width under the schedule, and the anomaly-1 fix does not close it: a distinct liveness stall. Hypothesis, not yet demonstrated: `reject_extra` (which raises `UnaskedReply` through `incoming.finish()`) runs only after the decode pump's question loop closes, and under deep geometry the walk parks before that drain is reached, so the duplicated ending-reaction frame is never detected and both sessions deadlock. | the lane's scratch run at `ffedd47b`'s schedule over its fixes; the collision sweep at `e951b81e` | Open, correctness class (liveness): the next investigation on the collision branch at 28 bytes, after `p2-deep-geometry` merges; T162 item 10's `ci` membership waits on it. |
| p2-deep-geometry fresh-eyes round 1 | Peer-malformed errors that arrive by the proxy's route arm rather than its pump (`Stream(Decode(codec kinds))`, `Mislabeled`, `UnaskedReply`, `TerminalQuery`, and the local-bug family) still take the terminal's wildcard arm and are rewritten to `SupplyClosed` when a supply deposit exists in the same wave; the sibling test `understated_target_message_size_fails_the_session` (asserting `OverbatchedRun`) is exposed to the same masking. Pre-existing; the lane's fix narrows it for the pump path only. | the reviewer's reading of `remote/proxy/work.rs` at `4c3051ed` | Closed when the branches meet: the vanish-liveness lane's T165 predicate (only supply-caused symptoms are outranked) applies to every arm; the coordinator integrates the two lanes' `execute` edits toward that one predicate and adds the same-wave test for the route path. |

