# Review packet: link-transport → main

A point-in-time reading aid for a 72-commit, 198-file branch: the tally, a
suggested reading order by semantic risk rather than chronology, and a
per-series walkthrough (A–H) of what each series changed and what already
survived its own adversarial round. Useful now mainly as a map of how that
campaign was structured and what it measured.

- [`review-packet-link-transport.md`](review-packet-link-transport.md) — the
  tally, the reading order, series A–H, cross-cutting facts, and the measured
  outcome of the whole campaign.

Every commit hash in the packet resolves on the archive branch
`wave1/integration`, not on `link-transport`; the body's own header says so.

---

Resurrected from `design/review-packet-link-transport.md`, written 2026-07-18, retired in `e13854de` (Remove outdated design docs). The body below is verbatim: its `design/…` cross-references resolve through the [migration map](../2026-08-19-design-directory-migration/README.md).
