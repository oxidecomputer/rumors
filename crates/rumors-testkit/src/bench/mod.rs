//! Models and fixtures shared by benchmarks and performance tests.

/// Fixtures spanning the reconciliation divergence grid.
pub mod grid;

/// An in-memory link with deterministic simulated latency.
pub mod latency;
